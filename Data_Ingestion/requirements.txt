# ── requirements.txt ──────────────────────────────────────────────────────────

# Document parsers
pymupdf>=1.24.0              # PDF (fitz)
python-docx>=1.1.0           # Word DOCX
python-pptx>=0.6.23          # PowerPoint PPTX
openpyxl>=3.1.0              # Excel XLSX

# Data / schema
pydantic>=2.0.0
python-dotenv>=1.0.0

# Azure
azure-functions>=1.19.0
azure-storage-blob>=12.20.0
azure-cosmos>=4.6.0
azure-identity>=1.16.0
azure-keyvault-secrets>=4.8.0

# Google ADK + LiteLLM
google-adk[extensions]>=1.0.0
litellm>=1.75.5
google-auth>=2.29.0
google-auth-httplib2>=0.2.0

# HTTP
requests>=2.31.0
httpx>=0.27.0

# XML / HTML parsing (used by DOCX inline image extraction)
lxml>=5.0.0

# Generation module — database ORM
sqlalchemy>=2.0.0

# Document export
markdown2>=2.4.0       # Markdown → HTML (used in PDF export)
# weasyprint>=60.0     # HTML → PDF (optional; uncomment for PDF support)